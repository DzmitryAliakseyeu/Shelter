import { sliderContainer } from "./constants.js";
import { pets } from "./objects.js";
import randomArr from "./randomFunction.js";
let arrPrevSlides = [];
export function addNewSliderBlock(){
    // console.log(pets.length)
    // console.log(randomArr(0, pets.length - 1))
    //     let index = randomArr(0, pets.length - 1);
    //     console.log(index)
    //     if(arrPrevSlides.includes(index)){
    //         index = randomArr(0, pets.length - 1);
    //     }
    //     arrPrevSlides.push(index);
        // const newSliderBlock = document.createElement('div');
        // newSliderBlock.classList.add('slider__block');
        // sliderContainer.append(newSliderBlock);
        // const imageSliderBlock = document.createElement('img');
        // newSliderBlock.prepend(imageSliderBlock);
        // imageSliderBlock.setAttribute('src', pets[index].imgSrc);

}

